<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="po.mybus.com">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:screenOrientation="portrait"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key"/>

        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.Cust">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" android:theme="@style/AppTheme.Cust"/>
        <activity android:name=".module.Jadwal" android:theme="@style/AppTheme.Cust" android:noHistory="true"/>
        <activity android:name=".module.BusTersedia" android:theme="@style/AppTheme.Cust" android:noHistory="true"/>
        <activity android:name=".module.Promo" android:theme="@style/AppTheme.Cust" android:noHistory="true"/>
        <activity android:name=".module.Order" android:theme="@style/AppTheme.Cust" android:noHistory="true"/>
        <activity android:name=".module.OrderDetail" android:theme="@style/AppTheme.Cust" android:noHistory="true"/>
        <activity android:name=".module.Pengaturan" android:theme="@style/AppTheme.Cust" android:noHistory="true"/>
    </application>

</manifest>